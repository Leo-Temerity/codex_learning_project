# Pomodoro Dashboard

A static Chrome-friendly Pomodoro timer with a dashboard layout, configurable focus rounds, persistent timer settings, completion sound, and optional browser notifications.

## Run locally

Open `index.html` in Chrome, or serve the folder locally for notification support:

```sh
python3 -m http.server 8000
```

Then visit `http://localhost:8000`.
